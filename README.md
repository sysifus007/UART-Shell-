# -
基于 STM32 单片机实现类 Linux 风格的串口命令行交互界面。采用分层架构设计（应用命令层 / Shell 核心框架 / HAL 适配层），核心框架与硬件完全解耦。实现了命令注册分发、Tab 自动补全、命令历史记录（上下键浏览）、ANSI 彩色输出、参数解析（支持引号包裹）等功能。内置 16 个命令覆盖 LED 控制、传感器读取、系统信息等场景，支持命令动态注册/注销，可扩展性强。
